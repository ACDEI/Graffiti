import { FilterHomePubsPipe } from './filter-home-pubs.pipe';

describe('FilterHomePubsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHomePubsPipe();
    expect(pipe).toBeTruthy();
  });
});
